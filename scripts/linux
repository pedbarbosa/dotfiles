#!/bin/bash
# TODO: This script currently only works in Arch Linux, need to generalise it

# Include functions
source scripts/functions

# Install required packages
if [[ $UID == 0 ]]; then
    run pacman -S python-pip sudo
else
    run sudo pacman -S python-pip sudo
fi

